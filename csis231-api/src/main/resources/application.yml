server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/csis_231_db
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true
  sql:
    init:
      mode: always
